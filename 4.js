let legalNames = ("num, num1, newName, new_name, ");

alert("legal variable names " + legalNames);

// variable names must start from lower case, and not 
// start with capital letters , a;so cannot start with
// a numbers , the numbers can add in last, you can use
// hyphens in middle.